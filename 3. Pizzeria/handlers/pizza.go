package handlers

import "net/http"

func GetPizzasHandler(w http.ResponseWriter, r *http.Request) {
	// TODO: Return a list of pizzas in JSON format as the response body with a status code of 200 from the database
}
